type CustomNavLinkProps = {
    isActive:boolean,
    title:string,
    icon:string
}

type RequestFeatureFormInput = {
    feature: string;
    description: string;
};

export type {CustomNavLinkProps, RequestFeatureFormInput}